# Benchmark result policy

Large raw event logs are not bundled in this compact source archive. The SoftwareX manuscript uses the repeated write-only runs summarised in `artifacts/softwarex_evidence/`.

The legacy pilot bundled in older alpha releases has been removed from this archive because it predates the final write-only benchmark protocol and evidence-aware rule semantics.
